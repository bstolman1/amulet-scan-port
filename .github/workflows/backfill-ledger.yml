name: Backfill Ledger History

on:
  schedule:
    - cron: '0 0 * * 0'  # Run every Sunday at midnight (00:00 UTC)
  workflow_dispatch:
    inputs:
      parallel_mode:
        description: 'Enable parallel processing (8 workers)'
        required: false
        type: boolean
        default: false
      migration_id:
        description: 'Specific migration ID to backfill (optional, leave empty for all)'
        required: false
        type: string

# Prevent concurrent runs that could conflict with database writes
concurrency:
  group: ledger-ingestion
  cancel-in-progress: false # Queue instead of canceling

jobs:
  backfill:
    runs-on: self-hosted
    strategy:
      fail-fast: false
      max-parallel: ${{ inputs.parallel_mode && 8 || 1 }}
      matrix:
        worker_id: ${{ inputs.parallel_mode && fromJson('[0,1,2,3,4,5,6,7]') || fromJson('[0]') }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          npm init -y
          npm install axios @supabase/supabase-js pg pg-copy-streams

      - name: Run backfill worker
        env:
          BASE_URL: https://scan.sv-1.global.canton.network.sync.global/api/scan
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
          BACKFILL_PAGE_SIZE: "200"
          WORKER_ID: ${{ matrix.worker_id }}
          WORKER_COUNT: ${{ inputs.parallel_mode && 8 || 1 }}
          MIGRATION_ID: ${{ inputs.migration_id || '' }}
        run: |
          if [ "${{ inputs.parallel_mode }}" = "true" ]; then
            echo "🚀 Starting parallel backfill worker ${{ matrix.worker_id }} of 8"
          else
            echo "🚀 Starting single-threaded backfill"
          fi
          node scripts/fetch-backfill-history.js
        timeout-minutes: 1440  # 24 hours max per worker

  verify:
    needs: backfill
    runs-on: self-hosted
    steps:
      - name: Report completion
        run: |
          echo "✅ Backfill complete"
          if [ "${{ inputs.parallel_mode }}" = "true" ]; then
            echo "📊 All 8 parallel workers finished"
            echo ""
            echo "⚠️  IMPORTANT: Don't forget to run scripts/restore-after-backfill.sql"
            echo "   to restore indexes and make tables LOGGED again!"
          fi
