# Server configuration
PORT=3001

# Data directory - path to ledger_raw folder (read-only access)
# Windows: DATA_DIR=C:/ledger_raw
# WSL:     DATA_DIR=/mnt/c/ledger_raw
# Linux:   DATA_DIR=/path/to/ledger_raw
DATA_DIR=C:/ledger_raw

# Optional: Groups.io API key for governance announcements
# GROUPS_IO_API_KEY=your_api_key_here

# Optional: Kaiko API key for market data
# KAIKO_API_KEY=your_api_key_here

# Optional: API authentication (recommended for production)
# API_KEY=your_secure_api_key_here

# Logging level (error, warn, info, debug)
LOG_LEVEL=info

# Note: Ingestion is handled separately by scripts/ingest/
# Run via: node scripts/ingest/fetch-updates.js
#          node scripts/ingest/fetch-backfill.js
#          node scripts/ingest/fetch-acs.js
